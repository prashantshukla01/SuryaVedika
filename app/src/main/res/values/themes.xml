<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Soni_innogeek" parent="Theme.Material3.DayNight.NoActionBar"></style>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    <!-- Dark mode theme -->
    <style name="AppTheme.Dark" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize dark mode attributes -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/black</item>
        <!-- Define other dark mode attributes -->
    </style>
    <style name="nightModeSwitch" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/light_magenta</item>
        <item name="colorSwitchThumbNormal">@color/white</item>
        <item name="android:colorForeground">#FFFFFF</item>

    </style>

    <style name="Theme.Soni_innogeek" parent="Base.Theme.Soni_innogeek" />
</resources>